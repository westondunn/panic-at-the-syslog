[project]
name = "panic-at-the-syslog"
version = "0.1.0"
requires-python = ">=3.12"

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.pytest.ini_options]
addopts = "-q -m 'not e2e'"
testpaths = ["services", "libs", "tests"]
markers = [
  "e2e: marks end-to-end smoke tests",
]
