# Contracts

Contracts are the source of truth for cross-service boundaries.

- `events/`: JSON Schemas for event envelopes.
- `api/openapi.yaml`: API stub.
- `prompts/`: Prompt and structured-output placeholders.
- `eval/fixtures/`: Validation fixtures used by CI.