# OSI-only core rules
- Tier 1 must remain OSI-approved open source.
- Non-OSI dependencies only allowed behind optional plugins/adapters:
  - disabled by default
  - clearly documented
  - excluded from CI/e2e requirements