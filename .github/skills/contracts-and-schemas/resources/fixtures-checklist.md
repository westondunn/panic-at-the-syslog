# Fixtures checklist
- Contains schema_version and correlation_id
- Uses stable IDs (event_id/finding_id/insight_id)
- Avoids real internal IPs/hostnames (use RFC5737 examples or placeholders)
- Represents real-world syslog quirks (missing fields, varied severity)